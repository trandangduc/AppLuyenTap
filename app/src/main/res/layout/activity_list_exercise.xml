<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- FrameLayout để chứa ImageView và TextView chồng lên nhau -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="200dp">

        <!-- Hình ảnh nền -->
        <ImageView
            android:id="@+id/imageView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/your_image"
            android:scaleType="centerCrop" />

        <!-- TextView hiển thị thời lượng và số lần tập, chồng lên hình ảnh -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="70dp"
            android:layout_marginEnd="100dp"
            android:background="#80000000"
            android:padding="8dp"
            android:text="20 phút • 7 Lần tập"
            android:textColor="#FFFFFF"
            android:textSize="18sp" />

    </FrameLayout>

    <!-- RecyclerView để hiển thị danh sách bài tập -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Nút bắt đầu bài tập -->
    <Button
        android:id="@+id/startButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Khởi đầu"
        android:backgroundTint="#0000FF"
        android:textColor="#FFFFFF" />

</LinearLayout>